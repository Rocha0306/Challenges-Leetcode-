#include <stdio.h>

int main(void) {
  int contador = 0; 
  int arrSize;
  int arr[arrSize];
   printf("Digite o tamanho do array: ");
    scanf("%d", &arrSize);



    for(int i = 0; i < arrSize; i++) {
        printf("Digite um numero: ");
        scanf("%d", &arr[i]);
        

      
      
      


    }

  
  return 0;
}
